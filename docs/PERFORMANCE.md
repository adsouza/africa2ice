# Performance record

The step-2a walking-skeleton transfer-size and frame-rate measurements have not yet been recorded on
the named release reference machine. The implementation keeps this omission explicit so a local
development measurement is not mistaken for the release baseline required by `docs/DESIGN.md`.
